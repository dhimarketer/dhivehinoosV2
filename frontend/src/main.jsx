// Ensure React loads first by importing it directly in the entry point
// This guarantees vendor-react chunk loads before other chunks
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.jsx'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
